import "./chunk-5WRI5ZAA.js";

// node_modules/levenshtein-edit-distance/index.js
var codes = [];
var cache = [];
function levenshteinEditDistance(value, other, insensitive) {
  if (value === other) {
    return 0;
  }
  if (value.length === 0) {
    return other.length;
  }
  if (other.length === 0) {
    return value.length;
  }
  if (insensitive) {
    value = value.toLowerCase();
    other = other.toLowerCase();
  }
  let index = 0;
  while (index < value.length) {
    codes[index] = value.charCodeAt(index);
    cache[index] = ++index;
  }
  let indexOther = 0;
  let result;
  while (indexOther < other.length) {
    const code = other.charCodeAt(indexOther);
    let index2 = -1;
    let distance = indexOther++;
    result = distance;
    while (++index2 < value.length) {
      const distanceOther = code === codes[index2] ? distance : distance + 1;
      distance = cache[index2];
      result = distance > result ? distanceOther > result ? result + 1 : distanceOther : distanceOther > distance ? distance + 1 : distanceOther;
      cache[index2] = result;
    }
  }
  return result;
}
export {
  levenshteinEditDistance
};
//# sourceMappingURL=levenshtein-edit-distance.js.map
